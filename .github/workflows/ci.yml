name: CI

on:
  pull_request:
    branches:
      - main
      - dev

permissions:
  contents: read

jobs:
  validate:
    name: Validate Node 24 (merge result)
    uses: ./.github/workflows/validate.yml
